"use client";

export default function ApiLimitNotification() {
  // This is a placeholder component to fix the build error
  // The original component was deleted, but references to it still exist
  return null;
} 